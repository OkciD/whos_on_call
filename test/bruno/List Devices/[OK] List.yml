info:
  name: "[OK] List"
  type: http
  seq: 7
  tags:
    - Device

http:
  method: GET
  url: "{{baseUrl}}/api/v1/devices?type=laptop&name=Workstation"
  params:
    - name: type
      value: laptop
      type: query
    - name: name
      value: Workstation
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Creates a new device for the authenticated user
