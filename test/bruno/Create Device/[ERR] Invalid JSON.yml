info:
  name: "[ERR] Invalid JSON"
  type: http
  seq: 5
  tags:
    - Device

http:
  method: POST
  url: "{{baseUrl}}/api/v1/device"
  body:
    type: json
    data: lolkek
  auth: inherit

settings:
  encodeUrl: true

docs: Invalid JSON request body
